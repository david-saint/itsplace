<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="base">
	<typeAlias type="net.itsplace.domain.Bascd" alias="Bascd"/>
	
	<select id="getGrpBascdList" resultClass="Bascd" >
    	 Select GRPCD,  GRPNAME  From PGRPBASCD Where USEYN='Y';	    	
    </select>  
	
    <select id="getBascd" parameterClass="int">
    	SELECT BASECD, GRPCD, BASNAME, REMARK, ISDELETE
    	FROM PBASCD
    	WHERE NO = #no#
    </select>
    <insert id="saveBasCd" parameterClass="Bascd">	
    	Insert Into PBascd(BASECD, GRPCD, BASNAME, REMARK, ISDELETE)
    	VALUES ( #baseCd#, #grpCd#, #basName#, #remark#, 'N')
    </insert>
    <update id="editBasCd" parameterClass="Bascd">	
    	UPDATE PBascd
    		BASECD =  #baseCd#,
    		GRPCD = #grpCd#,
    		BASNAME = #basName#,
    		REMARK = #remark#,
    		ISDELETE = #isDelete#
    	WHERE NO = #no#;
    </update>
</sqlMap>